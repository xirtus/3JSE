# Vendor Router

For every non-trivial capability, apply the reuse ladder.\n\nReturn: capability, project solution, preferred provider, integration mode, reason, fallback.\n\nNever substitute generic primitives for an applicable named provider without stating why. Provider mode matters: `provider` may be integrated; `reference_implementation` should be studied/ported selectively; `harness_reference` is structure/ideas only.
