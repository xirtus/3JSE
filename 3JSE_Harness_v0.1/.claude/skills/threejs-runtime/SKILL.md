# Three.js Runtime

Three.js is the rendering/runtime substrate, not permission to rebuild existing provider systems. Keep core gameplay state decoupled from scene objects when practical. Prefer TypeScript, deterministic update loops, explicit resource disposal, instancing/LOD for scale, and glTF/GLB for authored 3D assets. R3F is optional integration, not mandatory runtime architecture.
